<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= image_tag @user.image.thumb.url, :class => "thumbnail thumbnail_edit" %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <%= link_to '新たに施設を追加する', new_facility_path, class: 'btn btn-warning' %>
    <% if @user.facilities.any? %>
      <h3>現在<%= @user.facilities.count %>つの施設を管理しています</h3>
      <ol class="facilities">
        <%= render @facilities %>
      </ol>
    <% end %>
  </div>
</div>
